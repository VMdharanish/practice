class Solution:
	def pushZerosToEnd(self, arr):
    	# code here
    	res = 0
    	for i in range(len(arr)):
    	    if arr[i] !=0 :
    	         arr[res],arr[i] =  arr[i],arr[res]
    	         res+=1
